# 실기기 前 전면 디버깅 세션 — 버그 헌트 결과 (2026-07-25)

> 8-lens 멀티에이전트 finder → finding별 adversarial 반증 검증 → 직렬 TDD fix.
> finder 42건(중복 포함) → confirmed·수정 8건 + 반증 3건 + 백로그 아래 표.
> 원본 finding 전문은 세션 아카이브 (이 문서는 요약 + 백로그 SSoT).

## 1. 수정 완료 (커밋 단위)

| 심각도 | 결함 | 커밋 |
|---|---|---|
| **critical** | 시간 그리드 재진입 후 첫 sweep이 기존 투표 전체를 서버에서 삭제 (useSweepGesture selection 미시드 — 4개 lens 독립 발견) | c252489 |
| high | 연속 sweep의 commitVoteDiff 동시 발사 → INSERT/DELETE 착지 순서 역전 (유령 투표 부활) | 6c54653 |
| high | **세션 디스크 영속 부재 — 앱 재시작마다 로그아웃** (storage 어댑터 미주입 + bootstrap 복원 부재 + rejected promise 영구 캐시) | b868b93 |
| high | 히트맵 초기 스냅샷 부재 — 진입 시 다음 broadcast까지 타인 투표 빈 그리드 + polling 상태 실 fetch 미배선 | 703ea06 |
| medium | 투표 커밋 실패 시 diff 기준선 롤백 부재 → 실패분 영구 미전송 | 6c54653 |
| medium | midpoint userId hydration 전 조용한 no-op + groupId 부재 무가드 | ada0ed1 |
| medium | midpoint 출발지 A→B 정정 탭 순서 경합 + 동일 연타 중복 | aa1efd8 |
| low | calendar p_expires_at 직렬화 luxon 통일 (D13) / Deno 2.8 테스트 타입 / lint 신규 에러 | 2bd78e5 · f0e1e48 · 71a2d7a |

부가: 루트 ErrorBoundary 신설(1410561), EAS env manifest 정합성 테스트 + 누락 2키(295c514).

## 2. 반증 (수정 불요 — 근거 기록)

| 주장 | 반증 근거 |
|---|---|
| useMapSearch 이중 인스턴스 quota 낭비 | 두 인스턴스는 서로 다른 쿼리 스트림(자동 추천 vs 출발지 타이핑) — 동일 쿼리 중복 발사 경로 없음. 캐시 비공유는 재진입 시 miss뿐(측정 가능 수준 아님) |
| 자동추천 캐스케이드 (snap 변경마다 재발사) | snap은 최근접 역이 실제로 바뀔 때만 변경 + setQuery 동일 문자열 no-op + 400ms debounce 코얼레싱 |
| 추천 확정 더블 탭 이중 디스패치 (useState 가드 stale) | 별개 press 이벤트 사이에 React가 상태 커밋 → 가드 유효. 회귀 테스트로 고정 (d578ada) |

wontfix 재확인: stationSnap 942역 O(n) (midpoint 변경 시에만, <1ms), coldStart console.log (베타 logcat 판독 채널 — 의도).

## 3. 백로그 — 보안 (출시 전 필수, 별도 태스크)

> ⚠ 이 클러스터는 스키마·RPC 재설계가 필요해 이번 세션에서 수정하지 않음.
> anon key는 공개 배포물이므로 안암 invite-only 출시 전 반드시 해소.

| # | 결함 | 위치 | 비고 |
|---|---|---|---|
| SEC-1 | **anon이 users 전체 SELECT 가능** — `users_select_visible`에서 `auth.uid()=NULL` → `NOT is_blocked(NULL,id)`=true. 닉네임·카카오ID·프로필 전량 덤프 가능 | 0002_rls.sql:19 | M5 백로그 "users_select_visible anon 점검"과 동일 항목. 단순 `auth.uid() IS NOT NULL` 게이트는 **web-guest 방장·멤버 닉네임 join을 깨뜨림** (page.tsx:27, ClientPage.tsx:65) → SECURITY DEFINER RPC(get_guest_group_view)로 게스트 페이지 필요분만 노출 후 게이트 |
| SEC-2 | **guest_token 공개 노출 → 타 게스트 투표 위변조** — `group_guests_select_anon USING(true)`가 token 컬럼 노출, votes.guest_token도 동일. 수확한 token으로 save_guest_votes 호출 가능 | 0004_web_guest_rls.sql:23,36 | 근본 원인: token이 비밀(자격증명)이자 공개 식별자(투표 키잉, ClientPage.tsx:59,71)로 이중 사용됨. 처방: 공개용 guest_id 컬럼 도입 + token은 RPC 인자로만 + 컬럼 권한 축소. web-guest 재진입(NicknameForm.tsx:39 token 필터)도 RPC화 필요 |
| SEC-3 | groups 전체 덤프 — `groups_select_anon USING(true)` (UUID 링크로 개별 조회만 필요한데 전 테이블 스캔 허용) | 0004:14 | SEC-1 RPC 도입 시 함께 drop |
| SEC-4 | notify_publish 호출자 신원·관계 미검증 + is_blocked 미적용 — 스푸핑/차단 우회 push | notify_publish/index.ts:180 | Edge 배포됨(2026-07-25) — 우선순위 상향 |
| SEC-5 | attribution_resolve invite_code 경로 is_blocked 미검사 — 차단 사용자 모임 자동 합류 | attribution_resolve/index.ts:221 | |

## 4. 백로그 — 신뢰성·UX (finder 제출, 미검증 — 착수 시 반증 먼저)

| 심각도 | 항목 | 위치 |
|---|---|---|
| high | notify_f4 D17 게이트 비원자적 — 발송이 f4_sent_at claim **이전** (동시 호출 시 중복 push) | notify_f4/index.ts:193 |
| high | calendar_push_worker 부분 실패 retry 시 성공 멤버 중복 삽입 + queue claim/lease 부재 (pg_cron tick 중첩) | calendar_push_worker/index.ts:466,375 |
| medium | group_confirm F5 500 Response 버려져 실패가 fulfilled 집계 (확정 push 유실 silent) | group_confirm/index.ts:184 |
| medium | applePending insertEvent 성공 후 completed_at UPDATE 실패 시 다음 sync 중복 삽입 | src/lib/calendar/applePending.ts:97 |
| medium | 친구 탭 mount-only fetch — 수락 후 복귀 시 stale (focus refetch 누락) | app/(tabs)/friends/index.tsx:66, requests.tsx:158 |
| medium | 약관·온보딩 플래그 기기 전역 — 계정 전환 시 약관 게이트 skip (설계 결정 필요) | src/lib/auth/authStore.ts:113 |
| medium | midpoint/snap 소멸 시 자동 검색어·추천 미초기화 | app/group/[id]/midpoint.tsx:216 |
| medium | click_through 에러 message 고정 문자열 → 401 분기 사장 | src/lib/analytics/click_through.ts:101 |
| low | 온보딩 complete() 실패 시 submitting 영구 true | app/(auth)/onboarding.tsx:161 |
| low | 확정 성공 후 refetch가 같은 try 안 — 블립 시 성공+에러 토스트 동시 | app/group/[id]/index.tsx:253 |
| low | confirmGroup 403/401 분기 사장, 일정 지도 에러 재시도 부재, 요일 로케일(en 폴백), 24:00 종료가 확정 카드에서 00:00 표기 | confirm.ts:40 · schedule/map.tsx:218,46 · ConfirmedTimeCard.tsx:41 |

## 5. 백로그 — 접근성·카피 (일괄 처리 후보)

- OriginInput 검색 에러 무음 (에러 카피·재시도 UI 부재) — OriginInput.tsx:35
- PlaceActionSheet accessibilityViewIsModal 부재 + backdrop role 누락 — PlaceActionSheet.tsx:174
- 출발지·중간지점 검색 TextInput accessibilityLabel 누락 2곳 — OriginInput.tsx:49, midpoint.tsx
- 일정 지도 칩·토글 accessibilityState selected 누락 — schedule/map.tsx:112
- D41 privacy 구카피("기기 내 보관") 부재 단언 테스트 없음 — privacy.test.tsx

## 6. 세션 부가 발견 (환경)

- web-guest Playwright: 첫 실행 flake (webServer 첫 Turbopack 컴파일 경합 추정, 재현 불가) + 외부 서버 재사용 시 워커 teardown 300s 행 2회 — **Playwright 관리 spawn이 클린 (18/18, 15.2s)**
- Deno 2.8/TS 6.0 업그레이드로 Edge 테스트 스위트가 타입체크 단계에서 깨져 있었음 (수정됨). SESSION_LOG의 "Deno 8/8"은 부분 실행 기록 — 실제 스위트는 24파일 345 tests
- maestro CLI 미설치 — E2E yaml 4종 실행 불가 (실기기 세션 전 설치 권장)
- Edge secrets 완비 확인 (GEMINI·HMAC·KAKAO_REST·NAVER), anon group_origins 쓰기 401 거부 확인
